import discord
from discord.ext import commands
from dotenv import load_dotenv
import os

load_dotenv()
TOKEN = os.getenv("DISCORD_TOKEN")

bot = commands.Bot(command_prefix="!")

@bot.event
async def on_ready():
    print(f"✅ Bot is online as {bot.user.name}")

@bot.command()
async def hello(ctx):
    await ctx.send("Hey there, hero! 🕸️")

bot.run(TOKEN)
